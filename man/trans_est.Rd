% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidy.R
\name{trans_est}
\alias{trans_est}
\title{Transform and rescale estimate columns}
\usage{
trans_est(data, transform = exp, scale = 1)
}
\arguments{
\item{data}{A data frame containing columns \code{est}, \code{lwr}, and \code{upr}.}

\item{transform}{A function applied to \code{est}, \code{lwr}, and \code{upr}
(set to \code{NULL} to skip). Default \code{exp}.}

\item{scale}{Numeric scale factor by which transformed columns are divided.
Default \code{1}.}
}
\value{
The input data frame with \code{est}, \code{lwr}, and \code{upr} transformed and rescaled.
}
\description{
Applies a transformation (e.g., \code{exp}) and optional rescaling to the columns
\code{est}, \code{lwr}, and \code{upr} of a data frame.
}
\examples{
d <- data.frame(est = log(100), lwr = log(80), upr = log(120))
trans_est(d)                # exp + no rescale
trans_est(d, transform = NULL, scale = 1000)  # no transform, rescale

}
\keyword{internal}
