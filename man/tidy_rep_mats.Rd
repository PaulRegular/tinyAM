% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidy.R
\name{tidy_rep_mats}
\alias{tidy_rep_mats}
\title{Tidy key report matrices}
\usage{
tidy_rep_mats(fit)
}
\arguments{
\item{fit}{A fitted TAM object as returned by \code{\link[=fit_tam]{fit_tam()}}.}
}
\value{
A named list of data frames (elements: \code{N}, \code{M}, \code{mu_M}, \code{F}, \code{mu_F}, \code{Z}, \code{ssb_mat}),
where each data frame has one column per dimension (e.g., \code{year}, \code{age}) and
a value column \code{est}.
}
\description{
Converts selected matrices in \code{fit$rep} to long (tidy) data frames
using \code{\link[=tidy_mat]{tidy_mat()}}.
}
\examples{
fit <- fit_tam(northern_cod_data, years = 1983:2024, ages = 2:14)
mats <- tidy_rep_mats(fit)
names(mats)
head(mats$N)

}
\seealso{
\code{\link[=tidy_obs_pred]{tidy_obs_pred()}}, \code{\link[=tidy_sdrep]{tidy_sdrep()}}, \code{\link[=tidy_pop]{tidy_pop()}}, \code{\link[=tidy_mat]{tidy_mat()}}
}
