% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidy.R
\name{tidy_sdrep}
\alias{tidy_sdrep}
\title{Tidy \code{sdreport} time series with confidence intervals}
\usage{
tidy_sdrep(fit, interval = 0.95)
}
\arguments{
\item{fit}{A fitted TAM object as returned by \code{\link[=fit_tam]{fit_tam()}}.}

\item{interval}{Confidence level in \verb{(0, 1)}; default \code{0.95}.}
}
\value{
A named list of data frames (one per series), each with columns:
\itemize{
\item \code{year}, \code{est}, \code{sd}, \code{lwr}, \code{upr} — after applying the chosen transform.
}
}
\description{
Extracts ADREPORTED time-series from \code{fit$sdrep}, computes normal-approximation
intervals, applies a transformation (default \code{exp}), and returns a list of
tidy data frames.
}
\details{
Assumptions:
\itemize{
\item All ADREPORTED series used here have length equal to \code{length(fit$dat$years)}.
\item Estimates are on the log scale and are transformed with \code{exp} via \code{\link[=trans_est]{trans_est()}}.
\item List element names are cleaned by removing a leading \code{"log_"} prefix.
}

The interval is constructed as \verb{est ± z * sd} with
\code{z = qnorm(1 - (1 - interval) / 2)}.
}
\examples{
fit <- fit_tam(cod_obs, years = 1983:2024, ages = 2:14)
trends <- tidy_sdrep(fit, interval = 0.9)
names(trends)
head(trends$ssb)

}
\seealso{
\code{\link[=trans_est]{trans_est()}}, \code{\link[=tidy_rep_mats]{tidy_rep_mats()}}, \code{\link[=tidy_pop]{tidy_pop()}}
}
