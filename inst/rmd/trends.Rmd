
Trends
================================================================================

Row {.tabset}
--------------------------------------------------------------------------------

### Abundance

```{r}
plot_trend(pop$abundance, color = ~model, colors = cols, ylab = "Abundance")
```

### Recruitment

```{r}
plot_trend(pop$recruitment, color = ~model, colors = cols, ylab = "Recruitment")
```

### SSB

```{r}
plot_trend(pop$ssb, color = ~model, colors = cols, ylab = "SSB")
```

### Abundance-at-age

```{r}
plot_trend(pop$N, color = ~model, colors = cols, frame = ~age, ylab = "N")
```


<!-- ### Stock-Recruit -->

<!-- ```{r} -->

<!-- ``` -->


Row {.tabset}
--------------------------------------------------------------------------------

### Average F

```{r}
plot_trend(pop$F_bar, color = ~model, colors = cols, ylab = "Average F")
```

### F-at-age

```{r}
plot_trend(pop$F, color = ~model, colors = cols, frame = ~age, ylab = "F")
```

### Selectivity

```{r}
plot_trend(pop$S, x = ~age, xlab = "Age", ylab = "Selectivity", frame = ~year,
           color = ~model, colors = cols)
```

### Average M

```{r}
plot_trend(pop$M_bar, color = ~model, colors = cols, ylab = "Average M")
```

### M-at-age

```{r}
plot_trend(pop$M, color = ~model, colors = cols, frame = ~age, ylab = "M")
```


